export default {
    data() {
      return {
        msg: 'hello'
      }
    }
  }

then print msg like <h1>{{msg}}</h1> //cool!!!  variable interpolation

some valid expressions
------------------------

{{ number + 1 }}
{{ ok ? 'YES' : 'NO' }}
{{ message.split('').reverse().join('') }}
